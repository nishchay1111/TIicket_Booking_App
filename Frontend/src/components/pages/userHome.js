import { Typography, Box } from '@mui/material';

const UserHome = () => {
  return (
    <Box>
      <Typography variant="h4">Upcoming Events</Typography>
      <p>Here is where your event cards will go!</p>
    </Box>
  );
};

export default UserHome;