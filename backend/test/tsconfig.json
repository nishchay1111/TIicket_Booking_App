{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "declaration": false,
    "rootDir": ".." // This tells the test config to look at the whole project
  },
  "include": ["**/*.ts", "../src/**/*.ts"] 
}