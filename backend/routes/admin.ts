import express, { Router } from 'express';

const router: Router = express.Router();

// You can leave this space empty for now!
// Example: router.get('/stats', (req, res) => { ... });

export default router;